You can use the <b>Sculpt Tool (S)</b> to edit path items easily.
While using the <b>Scale Tool (Q)</b> you can double click an item to reset it's scale.
Hold the "Shift" key down while using the <b>Rotate Tool (R)</b> to rotate items in increments of 45°.
On an imported pixmap image, navigate to <b>Tools > Image > Image Trace</b> to convert it to an SVG file.